import pandas as pd


class DataGenerationManager(object):

    def __init__(self, export_path):
        # TODO create a dimension to link warehouse with stores
        # TODO create a dimension to link warehouse with providers (with a delivery date)
        self.export_path = export_path

if __name__ == "__main__":
    print("Hello world")
